version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.aws
    image: citnow-backend:latest
    container_name: citnow_backend
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - LOG_LEVEL=INFO
      # Redis Cache Configuration
      - REDIS_URL=redis://redis:6379/0
      # Database connection (MongoDB Atlas URI)
      - MONGODB_URI=mongodb+srv://appu:Prasanna460@scanner.8qogm7e.mongodb.net/citnow_analyzer?retryWrites=true&w=majority
      - MONGODB_DB_NAME=citnow_analyzer
      # AWS S3 Configuration
      - USE_S3_STORAGE=true
      - AWS_STORAGE_BUCKET_NAME=your-citnow-reports-bucket
      - AWS_S3_REGION_NAME=us-east-1
      # JWT and security config
      - JWT_SECRET_KEY=69379b844f20d396b23565cbf7d3c002cfe593dec5642d3b9808dcd36830222b
      - JWT_ALGORITHM=HS256
      - JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440
      # Super Admin Configuration
      - SUPER_ADMIN_USERNAME=admin
      - SUPER_ADMIN_PASSWORD=admin123
      - SUPER_ADMIN_EMAIL=admin@gmail.com
      - FRONTEND_URLS=https://qualitylens.focusengineeringapp.com,http://3.110.138.205:8000,http://3.110.138.205,http://ec2-3-110-138-205.ap-south-1.compute.amazonaws.com:8000,http://ec2-3-110-138-205.ap-south-1.compute.amazonaws.com,http://localhost:3000,https://main.d26c7jks4sa4os.amplifyapp.com,https://qualitylens.focusengineering.in
      # ML settings
      - WHISPER_MODEL=base
      # CPU Resource Optimization (T3a.large: 2 vCPU, 8GB RAM)
      - MAX_WORKERS=4
      - CONCURRENCY_LIMIT=2
      - PROCESS_TIMEOUT_SECONDS=960
      - VIDEO_HARD_DEADLINE_SECONDS=900
    depends_on:
      - redis
    restart: always

  redis:
    image: redis:7-alpine
    container_name: citnow_redis
    restart: always

  caddy:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
    container_name: caddy_proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - backend
      - redis

volumes:
  caddy_data:
  caddy_config:

